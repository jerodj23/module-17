import User from './User'; // Import the User model
import Thought from './Thought'; // Import the Thought model

// Exporting models for easy access throughout the application
export { User, Thought };
